[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "cd backend && python manage.py collectstatic --noinput",
]

[phases.setup]
nixPkgs = ["python312", "gcc"]

[start]
cmd = "cd backend && python manage.py migrate && gunicorn nexus.wsgi:application --bind 0.0.0.0:$PORT --workers 4"
