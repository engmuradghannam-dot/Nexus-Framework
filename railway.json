{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash -c 'cd backend && pip install -r ../requirements.txt && python manage.py collectstatic --noinput'"
  },
  "deploy": {
    "startCommand": "bash -c 'cd backend && python manage.py migrate && gunicorn nexus.wsgi:application --bind 0.0.0.0:$PORT --workers 4'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
