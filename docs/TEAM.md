# فريق تطوير Nexus Framework

توزيع مهام فريق تطوير الذكاء الاصطناعي على المهام الحرجة المستخرجة من
`تقرير_مراجعة_Nexus-Framework.md`، كل عضو مختص بمجاله:

| العضو | الاختصاص | المهمة | الحالة |
|---|---|---|---|
| Ahmad | DevOps / Security | إزالة كلمة مرور superuser المكتوبة صراحة من Dockerfile و`.env.example` و README، وإلغاء `makemigrations` من سكربت الإقلاع الإنتاجي | ✅ منجز |
| Omar | Backend Architect | توثيق الفرق المتعمَّد بين `core.CompanyProfile` (المستأجر) و`industry.Company` (شركة مُحلَّلة) لحل الالتباس المذكور في التقرير (P2 #5) | ✅ منجز |
| Lama | Backend / Data | التحقق الفعلي من عمل `import_controls` و`import_erp_fields` (P1 #3) — الأمر يعمل بشكل صحيح تقنياً (100 صناعة، 415 form control)، لكن ملف Excel المصدر يحوي بيانات عيّنة فقط لصناعة الطيران؛ إكمال المكتبة الكاملة عبر الصناعات الأخرى هو عمل إدخال بيانات (data entry) وليس عيباً برمجياً | ✅ تحقّق / ⏳ بيانات ناقصة |
| Sara | Frontend | ربط آخر ثلاث عناصر وهمية في لوحة المعلومات ببيانات حقيقية من الـ API: المهام القادمة (PMO)، أداء المبيعات الشهري (الفواتير)، وتوزيع المشاريع (حسب الحالة الفعلية بدل قطاع وهمي) (P1 #2) | ✅ منجز |
| Rana | QA / Backend | كتابة مجموعة اختبارات pytest لوحدة المحاسبة `accounts` (القيد المزدوج، ترحيل القيود، ميزان المراجعة، القوائم المالية، دفتر الأستاذ العام، الموازنات) — أعلى وحدة خطورة وبدون تغطية سابقاً (P1 #4) | ✅ 21 اختبار جديد، الكل ناجح (111/111 إجمالاً) |

## الجولة الثانية

| العضو | الاختصاص | المهمة | الحالة |
|---|---|---|---|
| Rana | QA / Backend | اختبارات وحدة الفوترة `invoicing`: حساب الضريبة، تاريخ الاستحقاق، الترحيل المزدوج للمبيعات والمشتريات، تسجيل الدفعات، تقادم الذمم (aging)، وترميز QR الخاص بـ ZATCA | ✅ 13 اختبار جديد |
| Rana | QA / Backend | اختبارات محرك تقييم المخزون `stockledger`: FIFO / LIFO / المتوسط المتحرك، نقل المخزون بين المستودعات، تجميع التقييم لكل صنف | ✅ 11 اختبار جديد |
| Sara | Frontend / Performance | تحويل كل صفحات التوجيه (~40 صفحة) إلى `React.lazy` + `Suspense` بدل الاستيراد الفوري بالكامل (P2) | ✅ الحزمة الرئيسية 756KB → 406KB (-46%)، كل صفحة تحمَّل عند الحاجة فقط |

**إجمالي اختبارات backend بعد الجولة الثانية: 135/135 ناجحة** (كانت 55 بالتقرير الأصلي).

## الجولة الثالثة

| العضو | الاختصاص | المهمة | الحالة |
|---|---|---|---|
| Omar | Backend Architect | إصلاح فجوة `Invoice` الحقيقية المكتشفة بالجولة السابقة: إضافة حقل `company` (migration `0005_invoice_company`)، وتعديل `post_to_ledger`/`zatca_qr` ليستخدما شركة الفاتورة الفعلية أولاً بدل "أول شركة بترتيب الـ id" | ✅ منجز + اختبار ارتداد (regression test) |
| Rana | QA / Backend | اختبارات دورة أمر البيع الكاملة `selling`: حساب الإجماليات، التسليم all-or-nothing (يرفض التسليم الجزئي عند نقص أي صنف)، انتقالات الحالة (Draft→Submitted→Delivered) | ✅ 11 اختبار جديد |
| Rana | QA / Backend | اختبارات دورة أمر الشراء الكاملة `buying`: حساب الإجماليات، استلام المخزون متعدد البنود، انتقالات الحالة (Draft→Submitted→Received) | ✅ 10 اختبار جديد |

**إجمالي اختبارات backend الآن: 157/157 ناجحة.**

## متبقٍ من تقرير المراجعة (لم يُوزَّع بعد)

- تدوير/إلغاء توكن GitHub المكشوف سابقاً في المحادثات — إجراء يدوي على GitHub، خارج نطاق الكود.
- تنظيف تاريخ Git من كلمة المرور القديمة `ghannam2020` (تصحيح الكود الحالي تم، لكنها لا تزال بتاريخ commits سابقة). عملية حساسة (rewrite history + force-push) — تحتاج موافقة صريحة قبل التنفيذ.
- اختبارات لوحدات: التصنيع (manufacturing)، الموارد البشرية (hr)، CRM.
- تفعيل `CompanyScopedMixin` الكامل على `InvoiceViewSet` (حالياً محمي بـ tenant فقط، بدون عزل على مستوى الشركة) — تغيير سلوك صلاحيات، يحتاج مراجعة منفصلة قبل التفعيل حتى لا يقفل وصول مستخدمين حاليين.
- مراجعة متخصصة لصحة منطق المحاسبة/التصنيع تحت الحمل الفعلي.
